![Python Version](https://img.shields.io/badge/python-3.9-blue.svg)

## 🛠 Installation

To run this project, you will need **Python 3.9**.

If you are using **Ubuntu**, run the following commands to set up the environment:

```bash
# Add the deadsnakes PPA to get Python 3.9
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt update

# Install Python 3.9 and the virtual environment module
sudo apt install python3.9 python3.9-venv
